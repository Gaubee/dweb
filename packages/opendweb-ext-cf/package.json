{
  "name": "@jixo/opendweb-ext-cf",
  "version": "0.1.3",
  "description": "opendweb plugin — Cloudflare Tunnel exposure wizard (ingress push via API, end-to-end verification, optional cloudflared co-spawn)",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.mts",
      "default": "./dist/index.mjs"
    },
    "./opendweb-plugin": {
      "types": "./dist/cli.d.mts",
      "default": "./dist/cli.mjs"
    }
  },
  "scripts": {
    "build": "tsdown",
    "typecheck": "tsc --noEmit",
    "pack:dry": "npm run build && npm pack --dry-run && node -e \"const p=require('./package.json');const deps=p.dependencies??{};if(Object.keys(deps).length>0){console.error('unexpected runtime dependencies:',deps);process.exit(1)}else{console.log('runtime dependencies: none (bundled via devDependencies)')}\" && ! grep -Eq '^[[:space:]]*(import|export).*@clack' dist/*.mjs",
    "test": "npm run build && node --test --test-force-exit --test-concurrency=1 test/*.test.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Gaubee/dweb.git"
  },
  "devDependencies": {
    "@clack/prompts": "^1.7.0",
    "@types/node": "^26.4.0",
    "smol-toml": "^1.8.0",
    "tsdown": "^0.22.14",
    "typescript": "^5.6.0"
  }
}
